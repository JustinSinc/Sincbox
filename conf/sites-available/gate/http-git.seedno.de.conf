# redirect http://git.seedno.de/ to https://git.seedno.de/
server {
        listen 80;
        listen [::]:80;

        server_name git.seedno.de;
        include /etc/nginx/conf.d/utils.conf;

        # rewrite url
        return 301 https://git.seedno.de/$request_uri;
}
